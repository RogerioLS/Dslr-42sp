# 42 DSLR Delivery Contract

1. **Required Files in Root**:
   - `describe.py`
   - `histogram.py`
   - `scatter_plot.py`
   - `pair_plot.py`
   - `logreg_train.py`
   - `logreg_predict.py`

2. **Output Format**:
   - `houses.csv` must strictly match:
     ```csv
     Index,Hogwarts House
     0,Gryffindor
     1,Hufflepuff
     ...
     ```

3. **Performance Requirement**:
   - The accuracy evaluated on `dataset_test.csv` must be $\ge 98.0\%$ using standard accuracy score metrics.
